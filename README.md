# wiki-quiz-devan
To build a Frontend UI and an API in Python (FastAPI/Django) that accepts a Wikipedia article URL as input and automatically generates a quiz based on the article content using a Large Language Model (LLM).
